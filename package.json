{
  "name": "osm-cpp",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run --pool=threads",
    "logs": "curl -s http://localhost:5173/__osm-postman/logs || cat logs/osm-postman-client.log 2>/dev/null || echo 'No logs yet — run npm run dev and use the app'"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.1.0",
    "vitest": "^4.1.9"
  },
  "dependencies": {
    "@types/leaflet": "^1.9.21",
    "leaflet": "^1.9.4"
  }
}
